<UserControl x:Class="OMSApp.Views.AddNewItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Basket -->
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Basket" VerticalAlignment="Center" Margin="0,8"/>
        <ComboBox Grid.Row="0" Grid.Column="1"
                  ItemsSource="{Binding Baskets}"
                  SelectedItem="{Binding SelectedBasket}"
                  DisplayMemberPath="DisplayText"
                  Margin="0,8"/>

        <!-- Product -->
        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Product" VerticalAlignment="Center" Margin="0,8"/>
        <ComboBox Grid.Row="1" Grid.Column="1"
                  ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct}"
                  DisplayMemberPath="DisplayText"
                  Margin="0,8"/>

        <!-- Quantity -->
        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Quantity" VerticalAlignment="Center" Margin="0,8"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,16,0,0">
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="120" Padding="8,6"
                    Margin="0,0,40,0"
                    Background="#4A9B9B"
                    Foreground="White"
                    BorderThickness="0"/>
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="120" Padding="8,6"
                    Background="#4A9B9B"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
