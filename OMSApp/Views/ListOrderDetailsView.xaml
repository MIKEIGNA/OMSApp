<Window x:Class="OMSApp.Views.ListOrderDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OMSApp.Views"
        mc:Ignorable="d"
        Title="ListOrderDetailsView" Height="450" Width="800">
    <StackPanel Margin="10">

        <ComboBox ItemsSource="{Binding Baskets}"
              SelectedItem="{Binding SelectedBasket}"
              DisplayMemberPath="CustomerName"/>

        <DataGrid ItemsSource="{Binding BasketItems}"
              AutoGenerateColumns="False"
              Height="250">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product"
                Binding="{Binding Product.ProductName}" />
                <DataGridTextColumn Header="Quantity"
                Binding="{Binding Quantity}" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Add Item"
            Command="{Binding AddItemCommand}" />
    </StackPanel>
</Window>
