<Window x:Class="OMSApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OMSApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Baskets -->
        <ListBox ItemsSource="{Binding Baskets}"
             SelectedItem="{Binding SelectedBasket}"
             DisplayMemberPath="CustomerName" />

        <!-- Items -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Basket Items" FontWeight="Bold" FontSize="16"/>

            <DataGrid ItemsSource="{Binding BasketItems}"
                  AutoGenerateColumns="False"
                  Height="250">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product"
                    Binding="{Binding Product.ProductName}" />
                    <DataGridTextColumn Header="Quantity"
                    Binding="{Binding Quantity}" />
                </DataGrid.Columns>
            </DataGrid>

            <Button Content="Add Item"
                Command="{Binding AddItemCommand}"
                Width="120"
                Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
